/* Um programa para calcular o mdc
   segundo o algoritmo de Euclides. */ 

int gcd (void u, void v[])
{ if (v == 0) return u ;
  else return gcd(v,u-u/v*v);
  /* u-u/v*v == u mod v */
}

int main(void)
{ int x; int q;
  x = input (); y = input ();
  output(gcd(x, y));
}